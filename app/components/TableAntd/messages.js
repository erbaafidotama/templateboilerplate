/*
 * TableAntd Messages
 *
 * This contains all the text for the TableAntd component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TableAntd';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TableAntd component!',
  },
});
